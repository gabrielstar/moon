pool:
  vmImage: 'ubuntu-latest'

steps:
- task: Powershell@2
  inputs:
   targetType: 'filePath' # Optional. Options: filePath, inline
   filePath: parallelRunner.ps1
   arguments: ''
   workingDirectory: 'runner'
    
